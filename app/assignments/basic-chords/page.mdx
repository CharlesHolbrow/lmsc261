---
title: "LMSC-261 | Basic Chords"
---

# Basic Chords

**Due:** Thursday September 24, 5:00 PM

Build a **16-chord** progression in Python, add **one** extra feature, then turn it in with a **pull request**.

Finish [Hands-On \#1](/modules/python-hands-on-1/) first. If functions are unclear, read [Functions](/modules/python/#functions) on the Python Basics page.

Work **inside** your named folder.

## Steps

1. Using [GitHub Desktop](https://desktop.github.com/), open the [homework repo](https://github.com/berklee-epd/lmsc261-homework)
2. **Pull/Sync** the latest (do this before every new PR)
   - Current Branch → `main`
   - Fetch origin → Pull origin
3. Make a **branch** named `your-name-assignment-2`
   - Example: `ada-lovelace-assignment-2`
4. In **your** folder, make a new directory called `assignment-2`
5. Put `main.py` in it. Start with the functions below.
6. **Run** `main.py`. Change it so it prints **16 chords** as note names (`C4`, not `60`). Add **one** extra feature (see Ideas).
7. Copy the `README.md` template below. Fill in every heading.
8. **Commit** in GitHub Desktop — short message, then Commit
9. **Push** (upload) to GitHub
10. Click **Create Pull Request**
11. Title: `Your Name Homework 2`
12. **🚨 Do not click Merge 🚨**

## main.py

The functions below build chords. Try them out. Can you understand how they work?

```python
def build_major_triad(root_note_number):
    root = root_note_number
    third = root_note_number + 4
    fifth = root_note_number + 7
    return [root, third, fifth]

def build_minor_triad(root_note_number):
    root = root_note_number
    third = root_note_number + 3
    fifth = root_note_number + 7
    return [root, third, fifth]

def build_dominant_seventh(root_note_number):
    root = root_note_number
    third = root_note_number + 4
    fifth = root_note_number + 7
    seventh = root_note_number + 10
    return [root, third, fifth, seventh]

def note_name(note_number):
    notes = ["C", "C#", "D", "D#", "E", "F", "F#", "G", "G#", "A", "A#", "B"]
    list_index = note_number % 12
    letter = notes[list_index]
    note_octave = (note_number // 12) - 1
    return f"{letter}{note_octave}"

def numbers_to_names(note_numbers):
    notes = []
    for note_number in note_numbers:
        notes.append(note_name(note_number))
    return notes

chord_1 = build_major_triad(60)
chord_2 = build_minor_triad(62)
chord_3 = build_dominant_seventh(67)

for chord in [chord_1, chord_2, chord_3]:
    note_names = numbers_to_names(chord)
    print(note_names)
```

## Ideas

Add **one** extra feature. The bullets are suggestions; something else is fine.

This will take some independent research. We have not covered everything you need yet.

- Rewrite `numbers_to_names` with a [list comprehension](https://www.learnpython.org/en/List_Comprehensions)
- Make the chord-builder functions more [DRY](https://en.wikipedia.org/wiki/Don%27t_repeat_yourself) (Don't Repeat Yourself)
- Allow `note_name` to print flats (`Db`) or sharps (`C#`) using an additional `use_flats = True` parameter

## README.md

Use these headings. Short answers. GitHub should render them as a title and three sections.

```markdown
# Assignment 2: Basic Chords

## What I added

## What I still don't understand

## AI
```

Under **AI**, it must be extremely clear what (if anything) was written with AI. If you used none, write `None`. AI can help you add complexity, but you do not get extra points for a more elaborate program. The goal is to demonstrate what you learned. 

## Done when

```
your-name/
└── assignment-2/
    ├── main.py
    └── README.md
```

Your PR is open on GitHub. You did **not** merge it.

## Grading

This is **completion-based**. Full credit if you did the work.

| Requirement                                                        |  Points |
| -------------------------------------------------------------------| ------: |
| You submitted a PR with **correctly named** files and folders      |      15 |
| `main.py` runs and prints **16 chords** as note names (not numbers)|      20 |
| You added **one extra feature** (see Ideas)                        |      20 |
| README uses at least three headings, with answers under each       |      15 |
| The **AI** heading says what was written with AI (`None` is OK)    |      15 |
| New branch; PR is open and mergeable                               |      15 |
| **Total**                                                          | **100** |

Do not work on `main`. Do not merge the PR. Change only files in your folder.
